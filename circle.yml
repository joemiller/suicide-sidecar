---
machine:
  services:
    - docker

test:
  override:
    - make build

deployment:
  production:
    branch:
      - master
      - release-test
    commands:
      - docker login -p "$QUAY_PASSWD" -u "$QUAY_USER" -e "unused@unused" quay.io
      - make push
